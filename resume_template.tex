\documentclass[10pt,a4paper]{article}

% --- PACKAGES FOR STYLING ---
\usepackage[utf8]{inputenc}
\usepackage{newtxtext, newtxmath} % Use Times New Roman font
\usepackage[T1]{fontenc}
\usepackage[
  left=0.6in,
  right=0.6in,
  top=0.4in,
  bottom=0.4in
]{geometry}
\usepackage{hyperref}
\usepackage{enumitem} % For custom list formatting
\usepackage{titlesec} % For custom section formatting
\usepackage[normalem]{ulem} % For underlining (\uline)
\usepackage{xcolor}

% --- CUSTOMIZATION ---
\pagestyle{empty}
\definecolor{linkcolor}{rgb}{0.0, 0.0, 0.8}
\hypersetup{
    colorlinks=true,
    linkcolor=linkcolor,
    filecolor=linkcolor,      
    urlcolor=linkcolor,
    pdftitle={Resume},
}

% Turn off section numbering globally
\setcounter{secnumdepth}{0}

% Define the style for your main sections (e.g., EDUCATION)
% This creates BOLD, ALL-CAPS, UNDERLINED text.
\titleformat{\section}
  {\bfseries\large\MakeUppercase} % Font size reduced from \Large to \large
  {}
  {0em}
  {\uline} % The underlining command
\titlespacing{\section}{0pt}{6pt}{4pt} % Reduced vertical spacing

% Customize bullet points for tighter left alignment
\setlist[itemize]{
  leftmargin=1.2em, % Further reduced left margin for tighter alignment
  label=\textbullet,
  topsep=1pt, % Reduced spacing
  partopsep=0pt,
  itemsep=1pt, % Reduced spacing
  parsep=0pt
}


% --- DOCUMENT START ---
\begin{document}

% --- HEADER SECTION ---
\begin{center}
    % Font size for name reduced from \Huge to \LARGE
    {\LARGE\bfseries \VAR{name | upper} } \\[4pt]
    \VAR{phone} $\cdot$ \href{mailto:\VAR{email}}{\VAR{email}} $\cdot$ \href{\VAR{linkedin_url}}{\VAR{linkedin_url | replace("https://", "")}} $\cdot$ \href{\VAR{github_url}}{\VAR{github_url | replace("https://", "")}}
\end{center}

% --- SUMMARY SECTION ---
\vspace{2pt}
\VAR{summary}
% Added horizontal line after the summary
\vspace{4pt}
\hrule
\vspace{4pt}


% --- DYNAMIC SECTIONS ---
\section{Education}
\begin{itemize}
    \item[] \textbf{\VAR{education.university}} \hfill \VAR{education.duration} \\
    \VAR{education.degree} \\
    \textit{CGPA: \VAR{education.gpa}}
\end{itemize}

\section{Work Experience}
%# First, loop through the relevant experience
\BLOCK{for job in work_experience.relevant}
\begin{itemize}
    \item[] \textbf{\VAR{job.role}, \VAR{job.company}} \hfill \textit{\VAR{job.duration}}
    \begin{itemize}
    \BLOCK{for point in job.points}
        \item \VAR{point}
    \BLOCK{endfor}
    \end{itemize}
\end{itemize}
\BLOCK{endfor}

%# Then, loop through the other experience
\BLOCK{for job in work_experience.other}
\begin{itemize}
    \item[] \textbf{\VAR{job.role}, \VAR{job.company}} \hfill \textit{\VAR{job.duration}}
    \begin{itemize}
    \BLOCK{for point in job.points}
        \item \VAR{point}
    \BLOCK{endfor}
    \end{itemize}
\end{itemize}
\BLOCK{endfor}

\section{Academic Projects}
%# First, loop through relevant projects
\BLOCK{for project in projects.relevant}
\begin{itemize}
    \item[] \textbf{\VAR{project.name}} | \textit{\VAR{project.technologies}}
    \begin{itemize}
    \BLOCK{for point in project.description}
        \item \VAR{point}
    \BLOCK{endfor}
    \end{itemize}
\end{itemize}
\BLOCK{endfor}

%# Then, loop through other projects
\BLOCK{for project in projects.other}
\begin{itemize}
    \item[] \textbf{\VAR{project.name}} | \textit{\VAR{project.technologies}}
    \begin{itemize}
    \BLOCK{for point in project.description}
        \item \VAR{point}
    \BLOCK{endfor}
    \end{itemize}
\end{itemize}
\BLOCK{endfor}

\section{Technical Skills}
\begin{itemize}
    \BLOCK{for category, skills_list in skills.items()}
        \item \textbf{\VAR{category.replace('_', ' ')}:} \VAR{skills_list}
    \BLOCK{endfor}
\end{itemize}

\section{Achievements}
\begin{itemize}
\BLOCK{for achievement in achievements}
    \item \VAR{achievement}
\BLOCK{endfor}
\end{itemize}

\end{document}